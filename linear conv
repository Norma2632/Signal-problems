clc;
close all;
clear all;
x = input ('Enter 1st sequence');
h = input ('Enter 2nd sequence ');
y = conv (x,h);
subplot(3,1,1);
stem(x);
xlabel('samples');
ylabel('amplitude');
subplot(3,1,3);
stem(y);
xlabel('samples');
ylabel('amplitude');
title('Linear convolation');
